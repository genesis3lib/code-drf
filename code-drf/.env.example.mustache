# Django REST Framework Backend for {{{projectName}}}
# Most settings use smart defaults - only override if needed

# Optional: Override Django secret key in production
# DJANGO_SECRET_KEY=your-secret-key-here

# Optional: Database configuration (when using extension-rdbms)
# DATABASE_URL=postgresql://user:password@localhost:5432/{{{projectName}}}
