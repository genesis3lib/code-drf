# Docker Compose for Local Development
# Generated by Genesis3 - code-drf module

services:
  backend:
    build:
      context: ./{{{backendModule.moduleId}}}
      dockerfile: Dockerfile.dev
    container_name: {{{projectName}}}-backend
    ports:
      - "8000:8000"
    volumes:
      - ./{{{backendModule.moduleId}}}:/app
      - backend_venv:/app/.venv
    environment:
      - DJANGO_SETTINGS_MODULE=prj_{{{projectName}}}.settings.dev
    command: python manage.py runserver 0.0.0.0:8000

volumes:
  backend_venv:
